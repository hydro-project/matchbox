---
source: src/test.rs
expression: "snapshot! { match () { mb!(&**mb!(&**x)) => () } }"
---
fn main() {
    match () {
        a0 if {
            #[allow(unused_variables)]
            if let mb!(& * * x) = &**a0 { true } else { false }
        } => {
            if let mb!(& * * x) = &**a0 {
                ()
            } else {
                panic!(
                    "Two invocations of Deref::deref returned different outputs on same inputs",
                )
            }
        }
    }
}
